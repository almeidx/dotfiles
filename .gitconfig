[user]
	name = 
	email = 
	signingkey = 
[gpg]
	format = ssh
[commit]
	gpgsign = true
[tag]
	gpgsign = true
[push]
	autoSetupRemote = true
[alias]
	ci = commit -m
	co = checkout
	cm = checkout main
	cb = checkout -b
	st = status -sb
	sf = show --name-only
	lg = log --pretty=format:'%Cred%h%Creset %C(bold)%cr%Creset %Cgreen<%an>%Creset %s' --max-count=30
	unstage = reset HEAD --
	undo = checkout --
	rb = reset --soft HEAD~1
	clean = git clean -df
	resignlast = rebase --exec 'git commit --amend --no-edit -n -S'
	incoming = !(git fetch --quiet && git log --pretty=format:'%C(yellow)%h %C(white)- %C(red)%an %C(white)- %C(cyan)%d%Creset %s %C(white)- %ar%Creset' ..@{u})
	outgoing = !(git fetch --quiet && git log --pretty=format:'%C(yellow)%h %C(white)- %C(red)%an %C(white)- %C(cyan)%d%Creset %s %C(white)- %ar%Creset' @{u}..)
	committers = !(git log | grep Author | sort | uniq -c | sort -n -r)
	resign = "!f() { git rebase --exec 'git commit --amend --no-edit -n -S' -i $1; }; f"
	rename = !git branch -m $(git rev-parse --abbrev-ref HEAD) $1
	amend = commit --amend --no-edit -m
	cim = !git commit -m \"$(curl -s https://whatthecommit.com/index.txt)\"
	cimc = !git commit -m \"chore: $(curl -s https://whatthecommit.com/index.txt)\"
[pull]
	rebase = true
